<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Blaine's Awesome Blog</title>
  </head>
  <body>
    <div class="container">
      <h1>Blaine's Awesome Blog</h1>
      <table>
      <% @entries.each do |entry| %>
    <div>
      <h2><a href="/single_entry/<%= id = entry.id %>"><%= entry.title %></a></h2>
      <p><%= entry.article %></p>
    </div>
    <% end %>
    <p><a href='http://localhost:9393/new_entry'>Add New Entry</a> </p>
    </table>
    </div>
  </body>
</html>












<!-- <div class="container">
  <h1>Welcome to Sinatra!</h1>
  <p>Hello, world!</p>

  <table>
    <tr>
      <th>File Description</th>
      <th>File Location</th>
    </tr>
    <tr>
      <td>This view file</td>
      <td><code><%= __FILE__.gsub(Sinatra::Application.root, '') %></code></td>
    </tr>
    <tr>
      <td>The layout file</td>
      <td><code><%= File.expand_path('../layout.erb', __FILE__).gsub(Sinatra::Application.root, '') %></code></td>
    </tr>
    <tr>
      <td>The current controller</td>
      <td><code><%= File.expand_path('../../controllers/index.rb', __FILE__).gsub(Sinatra::Application.root, '') %></code></td>
    </tr>
  </table>
</div> -->

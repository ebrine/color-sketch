<div class="container">
  <h1>Welcome to Anonymous Blog!</h1>
  <p>Hello, world!</p>
  <% if @entries %>
    <%= "it's working" %>
    <% @entries.each do |entry| %>
      <h2><%= entry.title %></h2>
      <p><%= entry.body %></p>
    <% end %>
  <% end %>

</div>
